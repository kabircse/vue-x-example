import { createStore } from 'vuex'
import userModule from './modules/user-module'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userModule
  }
})
